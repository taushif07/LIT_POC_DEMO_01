{
  "name": "therapist-widget",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist/ node_modules/",
    "reinstall": "npm run clean && npm install",
    "rebuild": "npm run reinstall && npm run build"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "vite-plugin-lit-css": "^2.1.0"
  }
}
